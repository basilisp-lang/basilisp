(in-ns 'basilisp.core)

(def list
  (fn* list [& args]
       (basilisp.lang.list/l args)))

(def first
  (fn* first [seq] (.-first seq)))

(def rest
  (fn* rest [seq] (.-rest seq)))

(def cons
  (fn* cons [v seq] (.cons seq v)))

(def identity
  (fn* identity [v] v))

(def print
     (fn* print [& args]
         (builtins/print args)))

(def meta
     (fn* meta [o]
         (.-meta o)))

(def with-meta
     (fn* with-meta [o meta]
         (.with-meta o meta)))
