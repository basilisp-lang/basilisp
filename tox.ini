[tox]
envlist = py36,mypy,lint

[testenv]
deps = pytest
commands =
    pytest --disable-warnings

[testenv:coverage]
deps =
    coveralls
    pytest
    pytest-cov
commands =
    pytest --disable-warnings --cov=src/basilisp --cov-report html
    coveralls

[testenv:mypy]
deps = mypy
skip_install = true
commands = 
    mypy --ignore-missing-imports --follow-imports=skip src/basilisp

[testenv:lint]
deps = prospector
commands =
    prospector --profile-path={toxinidir}